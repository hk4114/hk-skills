src/commands/install.ts logs single-match discovery with the skill name and resolved subpath (`info(\`Discovered 1 skill: ${candidate.name} ... at ${candidate.subpath}\`)`). tests/commands/install-discovery.test.ts validates the single-match flow completes and registers the skill with the correct subpath.
